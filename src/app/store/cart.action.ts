import { createAction } from '@ngrx/store';

export const add = createAction('[Cart Component] added');
export const remove = createAction('[Cart Component] removed');
export const reset = createAction('[Cart Component] Reset');